# !/bin/bash

cd "$(dirname "${BASH_SOURCE[0]}")/"
sudo cp ./Manwatchdog.gz /usr/share/man/man1/watchdog.1.gz
prog1=($(apt-cache policy cgroup-bin | head -n 2 | grep -o \(none\)))

if [ "$prog1." = "(none)." ]; then
sudo apt install cgroup-bin
else
echo 'totul ok'
fi

